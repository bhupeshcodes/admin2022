import dashboard from './dashboard';
import services from './services';
import work from './work';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, work, services]
};

export default menuItems;
